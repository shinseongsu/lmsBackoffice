spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/minicampus
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: '0000'

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true